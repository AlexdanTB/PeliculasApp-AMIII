import 'package:flutter/material.dart';

class ReproductorScreen extends StatelessWidget {
  const ReproductorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Reproductor de películas'));
  }
}
